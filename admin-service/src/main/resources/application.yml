server:
  port: 28085

spring:
  application:
    name: admin-service
  config:
    import:
      - nacos:shared.yml
      - nacos:admin-service.yml
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: public
      config:
        server-addr: 127.0.0.1:8848
        namespace: public
        file-extension: yml
