server:
  port: 28082

spring:
  application:
    name: user-service
  config:
    import:
      - nacos:shared.yml
      - nacos:user-service.yml
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: public
      config:
        server-addr: 127.0.0.1:8848
        namespace: public
        file-extension: yml
