server:
  port: 28081

spring:
  application:
    name: auth-service
  config:
    import:
      - nacos:shared.yml
      - nacos:auth-service.yml
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: public
      config:
        server-addr: 127.0.0.1:8848
        namespace: public
        file-extension: yml
